const mongoose = require('mongoose');
const Schema = require('mongoose').Schema;

// const BonusDictionary = new Schema(
//   {
//     bonus: {
//       type: Array,
//       default: [490, 80, , , gift]
//     }
//   },
//   { toJSON: { virtuals: true } }
// );

module.exports = BonusDictionary;
